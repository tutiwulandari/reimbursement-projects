import {render} from "@testing-library/react";
import {Component} from "react";

export default class HomeSideBar extends Component{
    render() {
        return(
            <div>
                <p> ini menu home sidebar</p>
            </div>
        )
    }
}